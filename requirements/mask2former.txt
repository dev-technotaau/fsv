# Requirements for Mask2Former + SegFormer-B5 Training
# Python >= 3.8
# CUDA >= 11.8 recommended

# NOTE: Standard library modules (json, time, traceback, random, sys, logging, 
#       datetime, typing, warnings, os, pathlib) are built into Python and 
#       do NOT need to be installed.

# Core PyTorch
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Transformers for Mask2Former
transformers>=4.30.0
timm>=0.9.0  # For SegFormer backbone

# Computer Vision
opencv-python>=4.8.0.74
Pillow>=10.0.0
albumentations>=1.3.1
scikit-image>=0.21.0

# COCO Dataset Support (Optional - only needed if using COCO JSON format)
pycocotools>=2.0.7  # For COCO dataset format and evaluation metrics

# Data Processing
numpy>=1.24.0
scipy>=1.11.0

# Training Utilities
tqdm>=4.66.0
tensorboard>=2.14.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Machine Learning
scikit-learn>=1.3.0
pandas>=2.0.0

# System Monitoring
psutil>=5.9.0
gpustat>=1.1.0

# Optional: Post-processing
# pydensecrf>=1.0.0  # Uncomment for CRF post-processing

# Optional: Flash Attention (for speedup if supported)
# flash-attn>=2.0.0  # Uncomment if GPU supports Flash Attention
